/*
 * Binary.hpp
 *
 *  Created on: Oct 11, 2023
 *      Author: XMF3944
 */

#ifndef _INCLUDE_FRAMEWORK_FILES_BINARY_HPP_
#define _INCLUDE_FRAMEWORK_FILES_BINARY_HPP_

namespace Files
{

	class Binary
	{
		public:
			/*
			 static bool Create( const String& fileName, uint32& handle );
			 static bool Open( const String& fileName, uint32 options, uint32& handle );

			 static uint32 Seek( uint32 handle, uint32 offset, ESeek typeSeek );
			 static bool Close( uint32 handle );

			 static uint32 Write( uint32 handle, cstr data, uint32 count );
			 static uint32 Read( uint32 handle, str data, uint32 count );

			 static bool Remove( uint32 handle, uint32 count );
			 static bool Insert( uint32 handle, const cstr data, uint32 count );

			 static bool Errase( const String& fileName );
			 static bool Exist( const String& fileName );

			 static bool Rename( const String& oldName, const String& newName );
			 static uint32 GetSize( const String& fileName );
			 */
	};

} /* namespace Files */

#endif /* SRC_FILES_BINARY_HPP_ */
